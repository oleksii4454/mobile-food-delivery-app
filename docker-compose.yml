services:
  postgres_db:
    image: postgres:15
    container_name: my_postgres_container
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: delivery_db
    ports:
      - "5432:5432"